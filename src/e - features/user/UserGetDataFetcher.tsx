import { useGetUserDataQuery } from "@/d - widgets/TestPage/api/surveyApi"
// import { useAppDispatch } from "@/g - shared/lib/store"
// import { useRef } from "react"

// const UserGetDataLoader : React.FC = () => {
//     const dispatch = useAppDispatch()
//     const {data, isSuccess, isError, isLoading} = useGetUserDataQuery()
//     const prevData = useRef<typeof data | null>(null);

// }