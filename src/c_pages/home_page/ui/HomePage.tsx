import { Home } from '@/d_widgets/home_page/ui/Home';
import { Layout } from '@/g_shared/ui/layout';

export const HomePage = () => {
    return (
        <Layout>
            <Home />
        </Layout>
    );
};
