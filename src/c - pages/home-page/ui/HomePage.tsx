import { Home } from '@/d - widgets/home-page/ui/Home';
import { Layout } from '@/g - shared/ui/layout';

export const HomePage = () => {
    return (
        <Layout>
            <Home />
        </Layout>
    );
};
